inputfile = 'videos/input.mov'       # The filename of the input video
framerate = 60          # The framerate for the output video
outputfile = 'videos/output3.mp4'     # The filename for the output file, the extension used here will be reflected in the format
outputcodec = 'libx264'       # The codec for the output file, the codec needs to be compatible with the file extension